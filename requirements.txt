# BeezDesktop dependencies
toga>=0.4.0
cryptography>=42.0.0
ecdsa>=0.18.0
mnemonic>=0.20
base58>=2.1.1
pyzmq>=25.1.0
requests>=2.31.0
Pillow>=10.0.0
briefcase>=0.3.26

# Smart RAG dependencies (client-side embedding & PDF support)
fastembed>=0.7.0    # ONNX-based local embedding generation
numpy>=1.26.0       # Embedding vector handling
pymupdf>=1.24.0     # PDF text extraction for Smart RAG indexing

# Shared module (install from parent)
# pip install -e ../shared/
